apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: app
  labels:
    app: exercise-10
    component: api
spec:
  selector:
    app: exercise-10
    component: api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
